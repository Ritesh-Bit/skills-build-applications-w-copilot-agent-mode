import ResourcePage from './ResourcePage'

export default function Users() {
  return <ResourcePage component="users" title="Members" eyebrow="THE COMMUNITY" description="People first, progress always." />
}