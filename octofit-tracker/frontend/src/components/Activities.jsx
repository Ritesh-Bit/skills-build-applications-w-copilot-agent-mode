import ResourcePage from './ResourcePage'

export default function Activities() {
  return <ResourcePage component="activities" title="Activities" eyebrow="MOVEMENT LOG" description="Every session counts. Keep the team pulse in view." />
}